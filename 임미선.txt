①②③④⑤⑥⑦⑧⑨
vi /etc/mysql/mysql.conf.d/mysqld.cnf
bind address,
mysqlx bind address 주석처리
워크벤치에서 root 비밀번호 입력
1일차
kopo34
\\192.168.23.153
교수님 이메일 : comics77@gmail.com

과제
1. 용어정리(손으로)
2. 실습(캡처+주석 PPT로)
3. 정리하기(손으로)
4. 수업시간 중 필기(손으로)

1. 네임 내이름
2. 서버이름
3. 서버 접속할 유저 아이디
4. 패스워드

su -
sudo apt update 
sudo apt upgrade
apt install apache2
apt install ssh

~ : 홈 디렉토리

2일차
*윈도우 초반에는 사용자와 관리자 개념이 없었다. (개인용으로만 만들어졌기 때문)
*원격으로 접속하기 위해 텔렛을 사용했는데 보안이 취약해서 요즘엔 안 씀.
요즘은 ssh를 쓴다.
*putty는 ssh의 클라이언트

*바깥에서 내부로 맵핑해주는것을 포트포워드라 한다
*DHCP 아이피 자동설정(ip, dns등을 자동으로 잡아줌)
*Static 아이피 수동설정
*사용자 하나당 하나의 프라이머리그룹을 갖고있다. 사용자를 삭제하기 전까진 프라이머리 그룹을 삭제할 수 없으며 사용자가 여러명일 경우 프라이머리 그룹도 여러개이다. 사용자를 삭제하면 프라이머리그룹도 삭제된다(사용자와 사용자의 프라이머리그룹은 짝꿍)
cat: 파일의 명령어를 보는 명령어

group은 권한에 대한 것. directory와는 다른 개념.

mkdir : 디렉토리를 만드는 명령어
rm -rf : 디렉토리 삭제하기(내용과 파일 모두 삭제)

page는 하지 말고 넘어가기

>하나는 덮어쓰기
>>붙여쓰기


<<< ** JAVA ** >>>

SW
--- App
--- Frameworks
--- Firmware(칩을 구동시키는 역할)

HW

하드웨어와 가장 가까운 건 펌웨어

paramiter = 인자
returnvalue = 결과값

인스턴스 필드와 메소드는 객체가 무조건 생성이 되어야 쓸 수 있다. (인스턴스는 객체 안에만 존재. 스태틱은 객체 밖 메소드 영역에 저장되어있음).
정적 메소드와 정적 블록 작성시에는 인스턴스를 쓸 수 없음.
객체 자신의 참조인 this 사용 불가.

getInstance() : 자기 자신의 객체를 리턴한다. 리턴 타입으로 자기 자신의 클래스를 넣는다.

